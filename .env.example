# JWT
JWT_SECRET=          # Secreto para firmar tokens JWT (obligatorio al iniciar)

# Base de datos (PostgreSQL)
DB_HOST=             # Host del servidor PostgreSQL
DB_PORT=5432         # Puerto PostgreSQL
DB_USER=             # Usuario de la base de datos
DB_PASSWORD=         # Contraseña de la base de datos
DB_NAME=             # Nombre de la base de datos

# CORS
FRONTEND_URL=        # URL del frontend permitida por CORS (ej: http://localhost:3000)

# Email SMTP
EMAIL_HOST=          # Host SMTP (ej: smtp.mailtrap.io en desarrollo)
EMAIL_PORT=          # Puerto SMTP (ej: 587)
EMAIL_USER=          # Usuario SMTP
EMAIL_PASS=          # Contraseña SMTP
